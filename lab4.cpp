//
// Created by Madinabonu Mirzaxmatova on 17/02/26.
//
#include <iostream>
#include <cmath>
using namespace std;
int main() {
   /* int a;
    cin >> a;
    int b;
    cin >> b;
    int c;
    cin >> c;
    int m=(a+b+c)/3;
    cout << "Var=" << float ((a-m)*(a-m)+ (b-m)*(b-m)+(c-m)*(c-m))/3;

    int N;
    cout << "Enter the number ";
    cin >> N;
    if (N > 0)
        cout << "positive";
    if (N<0)
        cout << "negative";
    else
        cout << "zero";

    int M;
    cout << "Enter a number = ";
    cin >> M;
    if (M%2 ==0)
        cout << "EVEN";
    else cout << "ODD";


    int A;
    cout << "enter 1st number ";
    cin >> A;
    int B;
    cout << "enter 2nd number ";
    cin >> B;
    if (A>B)
        cout << "A is largest";
    else if (B>A)
        cout << "B is largest";
    else
        cout << "Numbers are equal";

    int a;
    cout << "a = ";
    cin >> a;
    int b;
    cout << "b = ";
    cin >> b;
    int c;
    cout << "c = ";
    cin >> c;
    if (a > b && a > c)
        cout << "a is largest";
    else if (b > c && b > a)
        cout << "b is largest";
    else if ( c > a && c > a)
        cout << "c is the largest";
    else
        cout << "Numbers are equal";


    int N;
    cout << "enter the number";
    cin >> N;
    if (N < 20)
        cout << "too slow";
    else if (N >80)
        cout << "too fast";
    else
        cout << "just right";

    float N;
    cout << "Enter N ";
    cin >> N;
    if (N >= 50)
        cout << "pass";
    else
        cout << "fail";

    int  M;
    cout << "Enter the number ";
    cin >> M;
    int N;
    cout << "Enter the number ";
    cin >> N;
    if (M%N == 0)
        cout << M << " is divisible by " << N;
    else
        cout << M << " is not divisible by " << N;
*/
}